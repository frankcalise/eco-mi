/** Shared between compact sheet and tablet mode-select route (pulse + dismiss). */
export const MODE_PICKER_PULSE_DURATION_MS = 150
export const MODE_PICKER_PULSE_COUNT = 2
export const MODE_PICKER_DISMISS_DELAY_MS = 200
